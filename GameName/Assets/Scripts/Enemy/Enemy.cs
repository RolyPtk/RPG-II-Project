using UnityEngine;

public class Enemy : Entity
{
    // have fun
    // here it would be a nice ideea to add things only the enemy would need, we can delete this later if need be
    // we just need a script of attacking and decision making besides movement, like to attack which you can do by accessing the 
    // reachedEndOfPath variable of EnemyAI I think 
}